import * as React from "react"

const SpacingCard = ({ margin }) => {
  const spaceCardStyle = {
    marginTop: margin,
  }

  return <div style={spaceCardStyle}>{/* empty space here */}</div>
}

export default SpacingCard
